# Description
* A command line program which supports CRUD functions of notes
* Use Node.js modules to parse argument then determine which function should be executed
#### use --help to get more information
